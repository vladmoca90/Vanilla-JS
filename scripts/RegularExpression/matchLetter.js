//A function that returns true is a letter matches

function letterMatch(word) {
    if (word.length == 0) {
        throw new Error('The word must be provided');
    }

    // if(word.match('a'))

    // return true;
}