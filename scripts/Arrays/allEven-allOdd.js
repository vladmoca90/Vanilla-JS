// A function that returns true if all 3 elements are even

const allEven = ([a, b, c]) =>
  a % 2 === 0 && b % 2 === 0 && c % 2 === 0;

const allOdd = ([a, b, c]) =>
  a % 2 !== 0 && b % 2 !== 0 && c % 2 !== 0;